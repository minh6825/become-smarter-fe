import HistoryPage from '@/section/history'
import React from 'react'

type Props = {}

const page = (props: Props) => {
  return (
    <div>
        <HistoryPage />
    </div>
  )
}

export default page